<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//rnybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.honghua.dao.add.SQLadd">
    <insert id="reg" parameterType="com.honghua.dao.PO.User">
        insert into user value (#{id},#{name},#{sex},#{age},#{email},#{phone},#{psw})
    </insert>
</mapper>